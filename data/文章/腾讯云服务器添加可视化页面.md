---
title: 腾讯云服务器添加可视化页面
date: 2021-12-01 02:26:32
categories: default
tags: []
---
最近买了一台国内一年的腾讯云服务器，服务器没有可视化页面，不好挂载QQ类物品，同时可视化页面可以一直挂着ssh窗口。

输入以下代码进入linux可视化安装界面

**Ubuntu**

```auto
sudo apt-get install -y xauth xterm x11-common x11-xkb-utils xfonts-base xfonts-encodings xfonts-utils xserver-common
sudo apt-get install -y xvfb
sudo apt-get install -y x11vnc
x11vnc -rfbport 12345 -passwd CrekeNet -create -forever//端口为12345(开启端口需放行安全组）
```

**centos**

```auto
yum groupinstall "Server with GUI" -y
yum update//提示错误时使用
systemctl set-default graphical
```

然后重启一下，可以使用腾讯云官方的vnc连接。

但是腾讯云官方显示慢，这里推荐使用real下的vnc

地址：[https://www.realvnc.com/en/connect/download/viewer/](https://www.realvnc.com/en/connect/download/viewer/)

下载对应版本后，安装，输入用户名和密码即可使用
